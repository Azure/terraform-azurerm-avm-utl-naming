name: Refresh naming catalogs

on:
  schedule:
    - cron: "23 6 * * 1"
  workflow_dispatch:

permissions: {}

concurrency:
  group: resource-name-rules-publication
  cancel-in-progress: false

jobs:
  publish:
    if: >-
      github.repository == 'Azure/terraform-azure-avm-utl-naming' &&
      !github.event.repository.fork &&
      (github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') &&
      github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: write
      pull-requests: write
    defaults:
      run:
        shell: pwsh
    steps:
      - name: Check out the default branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.repository.default_branch }}
          fetch-depth: 0
          persist-credentials: false
      - name: Test parser and mocked publication
        run: .\scripts\tests\Test-ResourceNameRules.ps1
      - name: Install AVM authoring for generated-data validation
        run: Install-PSResource -Name Avm.Authoring -Repository PSGallery -TrustRepository
      - name: Regenerate runtime catalogs and propose changes
        env:
          GH_TOKEN: ${{ github.token }}
          NAMING_RULES_BASE_BRANCH: ${{ github.event.repository.default_branch }}
        run: .\scripts\Publish-ResourceNameRules.ps1
